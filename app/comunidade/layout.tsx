import type { Metadata } from "next";
export const metadata: Metadata = { title: "Pessoas e comunidade", description: "Tradições, agricultura familiar e memórias da comunidade do Rio da Casca." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
