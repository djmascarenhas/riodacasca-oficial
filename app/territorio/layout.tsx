import type { Metadata } from "next";
export const metadata: Metadata = { title: "Natureza e território", description: "Rio, cachoeiras, cerrado e conservação no território do Rio da Casca." };
export default function Layout({ children }: { children: React.ReactNode }) { return children; }
